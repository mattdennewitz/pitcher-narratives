---
phase: quick
plan: 260326-t0k
type: quick
---

# Quick Task 260326-t0k: Add -v verbose switch

## Goal
Add a `-v`/`--verbose` CLI flag that prints pitcher name, game dates, and pitch counts per game to stderr before sending data to the LLM.

## Tasks

### Task 1: Add -v flag and verbose output
- **files:** main.py, tests/test_cli.py
- **action:** Add `-v`/`--verbose` argparse flag, `_print_verbose_summary()` function, 4 new tests
- **verify:** `uv run pytest tests/test_cli.py -v` passes
- **done:** Flag parses, verbose output shows on stderr, report still streams to stdout
