---
phase: 23-remove-old-pipeline
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/pitcher_narratives/report.py
  - tests/test_report.py
autonomous: true
requirements: [REM-01, REM-02]

must_haves:
  truths:
    - "report.py does not exist in the repository"
    - "test_report.py does not exist in the repository"
    - "No module in the codebase imports from report.py"
    - "All tests pass after deletion"
    - "anchor.py is unchanged and still importable"
  artifacts:
    - path: "src/pitcher_narratives/report.py"
      provides: "MUST NOT EXIST (deleted)"
    - path: "tests/test_report.py"
      provides: "MUST NOT EXIST (deleted)"
    - path: "src/pitcher_narratives/anchor.py"
      provides: "Unchanged — still exports AnchorResult, AnchorWarning"
  key_links:
    - from: "src/pitcher_narratives/pipeline.py"
      to: "src/pitcher_narratives/anchor.py"
      via: "import AnchorResult, AnchorWarning"
      pattern: "from pitcher_narratives\\.anchor import"
---

<objective>
Delete the old single-agent report pipeline (report.py) and its tests (test_report.py), then verify no broken imports remain and all tests pass.

Purpose: Complete the removal of the old reporting infrastructure, making pipeline.py the sole report generation path.
Output: report.py and test_report.py deleted, clean test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-remove-old-pipeline/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete report.py and test_report.py</name>
  <files>src/pitcher_narratives/report.py, tests/test_report.py</files>
  <read_first>
    - src/pitcher_narratives/report.py (confirm this is the old pipeline file to delete)
    - tests/test_report.py (confirm this is the old pipeline test file to delete)
  </read_first>
  <action>
1. **Delete src/pitcher_narratives/report.py** — `rm src/pitcher_narratives/report.py`
2. **Delete tests/test_report.py** — `rm tests/test_report.py`
3. **Scan for any remaining imports from report.py:**
   ```bash
   grep -r "from pitcher_narratives.report import\|import pitcher_narratives.report" src/ tests/
   ```
   This MUST return zero matches. If any matches found, fix them (they should have been handled by plan 23-01, but double-check).
4. **Verify anchor.py is untouched:**
   ```bash
   git diff src/pitcher_narratives/anchor.py
   ```
   This MUST show no changes.
  </action>
  <verify>
    <automated>cd /Users/matt/src/pitcher-narratives && test ! -f src/pitcher_narratives/report.py && echo "report.py deleted" && test ! -f tests/test_report.py && echo "test_report.py deleted" && grep -r "from pitcher_narratives.report import" src/ tests/ 2>/dev/null; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/pitcher_narratives/report.py` succeeds (file does not exist)
    - `test ! -f tests/test_report.py` succeeds (file does not exist)
    - `grep -r "from pitcher_narratives.report import" src/ tests/` returns 0 matches
    - `git diff src/pitcher_narratives/anchor.py` shows no changes
  </acceptance_criteria>
  <done>report.py and test_report.py are deleted, no remaining imports from report.py anywhere in the codebase, anchor.py is unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify clean state</name>
  <files></files>
  <read_first>
    - src/pitcher_narratives/anchor.py (confirm it is importable and unchanged)
  </read_first>
  <action>
1. **Run the full test suite:**
   ```bash
   uv run python -m pytest -x -q 2>&1
   ```
   All tests must pass. The hallucination guard tests now live in tests/test_hallucination_guard.py (created in plan 23-01) and should pass. The old test_report.py tests for the old pipeline internals (_make_agents, _EDITOR_PROMPT, ReportResult, etc.) are intentionally gone — they tested old pipeline code that no longer exists.

2. **Verify anchor.py is importable and used:**
   ```bash
   uv run python -c "from pitcher_narratives.anchor import AnchorResult, AnchorWarning; print('anchor OK')"
   ```

3. **Verify pipeline.py imports work:**
   ```bash
   uv run python -c "from pitcher_narratives.pipeline import generate_pipeline_streaming, check_hallucinated_metrics, HallucinationReport; print('pipeline OK')"
   ```

4. **Verify CLI modules import cleanly:**
   ```bash
   uv run python -c "from pitcher_narratives.cli import main; print('cli OK')"
   uv run python -c "from pitcher_narratives.ask_cli import main; print('ask_cli OK')"
   ```

NOTE: If `tests/test_analyst.py` has a pre-existing import error (importing `_analyst_agent` which no longer exists), that is a pre-existing issue unrelated to this phase — note it in the summary but do not fix it here (out of scope per REQUIREMENTS.md).
  </action>
  <verify>
    <automated>cd /Users/matt/src/pitcher-narratives && uv run python -m pytest -x -q --ignore=tests/test_analyst.py 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m pytest -x -q --ignore=tests/test_analyst.py` shows all tests passed, 0 failures
    - `uv run python -c "from pitcher_narratives.anchor import AnchorResult, AnchorWarning"` succeeds
    - `uv run python -c "from pitcher_narratives.pipeline import generate_pipeline_streaming, check_hallucinated_metrics"` succeeds
    - `uv run python -c "from pitcher_narratives.cli import main"` succeeds
    - `uv run python -c "from pitcher_narratives.ask_cli import main"` succeeds
  </acceptance_criteria>
  <done>Full test suite passes (ignoring pre-existing test_analyst.py import error), all module imports clean, anchor.py intact, pipeline.py is the sole reporting path</done>
</task>

</tasks>

<verification>
1. `test ! -f src/pitcher_narratives/report.py` — file deleted
2. `test ! -f tests/test_report.py` — file deleted
3. `grep -r "from pitcher_narratives.report" src/ tests/` — zero matches
4. `git diff src/pitcher_narratives/anchor.py` — no changes
5. `uv run python -m pytest -x -q --ignore=tests/test_analyst.py` — all tests pass
6. `uv run python -c "from pitcher_narratives.pipeline import check_hallucinated_metrics, HallucinationReport"` — clean import
</verification>

<success_criteria>
- report.py is gone from the repository
- test_report.py is gone from the repository
- No module imports from report.py
- anchor.py is identical to before this phase
- Full test suite passes (excluding pre-existing test_analyst.py issue)
- All CLI entry points import cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/23-remove-old-pipeline/23-02-SUMMARY.md`
</output>
