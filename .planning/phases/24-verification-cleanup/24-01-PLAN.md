---
phase: 24-verification-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pitcher_narratives/anchor.py
  - src/pitcher_narratives/config.py
autonomous: true
requirements: [CLI-03, VER-01, VER-02]

must_haves:
  truths:
    - "Full test suite passes with zero failures (excluding pre-existing test_analyst.py import error)"
    - "anchor.py AnchorResult and AnchorWarning are importable and used by pipeline.py"
    - "CLI hallucination check runs automatically on pipeline output in cli.py"
    - "CLI --verbose flag prints pitcher data summary"
    - "CLI --print-prompts flag prints pipeline data file to stderr"
  artifacts:
    - path: "src/pitcher_narratives/anchor.py"
      provides: "Shared anchor check module with updated docstring"
      contains: "AnchorResult"
    - path: "src/pitcher_narratives/config.py"
      provides: "Shared configuration with updated docstring"
      contains: "API_KEYS"
    - path: "src/pitcher_narratives/pipeline.py"
      provides: "Sole report generation path with hallucination guard"
      exports: ["generate_pipeline_streaming", "check_hallucinated_metrics", "HallucinationReport"]
    - path: "src/pitcher_narratives/cli.py"
      provides: "Main CLI routing through pipeline.py"
      contains: "check_hallucinated_metrics"
  key_links:
    - from: "src/pitcher_narratives/pipeline.py"
      to: "src/pitcher_narratives/anchor.py"
      via: "from pitcher_narratives.anchor import AnchorResult, AnchorWarning"
      pattern: "from pitcher_narratives\\.anchor import"
    - from: "src/pitcher_narratives/cli.py"
      to: "src/pitcher_narratives/pipeline.py"
      via: "from pitcher_narratives.pipeline import check_hallucinated_metrics, generate_pipeline_streaming, write_pipeline_data_file"
      pattern: "from pitcher_narratives\\.pipeline import"
---

<objective>
Verify the post-removal codebase is clean and all CLI features work through the pipeline path.

Purpose: Final milestone verification -- confirm that Phase 23's removal of report.py left no broken tests, stale references, or disconnected CLI features. This closes out v1.9 Pipeline Consolidation.

Output: Passing test suite, verified CLI features, cleaned-up docstrings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-remove-old-pipeline/23-VERIFICATION.md

@src/pitcher_narratives/cli.py
@src/pitcher_narratives/pipeline.py
@src/pitcher_narratives/anchor.py
@src/pitcher_narratives/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify anchor/pipeline imports</name>
  <read_first>
    - src/pitcher_narratives/anchor.py (full file -- check AnchorResult, AnchorWarning exports)
    - src/pitcher_narratives/pipeline.py (lines 1-80 -- check anchor import at L69)
    - tests/test_hallucination_guard.py (full file -- confirm imports from pipeline.py)
  </read_first>
  <files>none (verification only)</files>
  <action>
    1. Run the full test suite: `uv run python -m pytest -x -q --ignore=tests/test_analyst.py`
       - test_analyst.py has a PRE-EXISTING import error (imports `_analyst_agent` which no longer exists) -- exclude it.
       - All other tests must pass with zero failures.

    2. Verify anchor.py imports work from pipeline.py:
       `uv run python -c "from pitcher_narratives.anchor import AnchorResult, AnchorWarning; print('anchor imports OK')" `

    3. Verify pipeline.py exports the hallucination guard:
       `uv run python -c "from pitcher_narratives.pipeline import check_hallucinated_metrics, HallucinationReport; print('hallucination guard imports OK')"`

    4. Verify cli.py imports cleanly:
       `uv run python -c "from pitcher_narratives.cli import main; print('cli imports OK')"`

    5. Verify ask_cli.py imports cleanly:
       `uv run python -c "from pitcher_narratives.ask_cli import main; print('ask_cli imports OK')"`

    6. Verify hallucination guard tests pass independently:
       `uv run python -m pytest tests/test_hallucination_guard.py -x -q`
       Expected: 17 passed.

    If any test fails (other than test_analyst.py), investigate and fix. Known pre-existing issue in test_pipeline.py: `TestAuditAndReviseSpecialists::test_clean_audit_returns_originals` has a pydantic-ai TestModel assertion error -- if this is the only failure, note it as pre-existing and continue.
  </action>
  <verify>
    <automated>cd /Users/matt/src/pitcher-narratives && uv run python -m pytest -x -q --ignore=tests/test_analyst.py</automated>
  </verify>
  <acceptance_criteria>
    - pytest reports 0 failures (excluding test_analyst.py which is ignored)
    - All 5 import verification commands print their OK messages
    - 17 hallucination guard tests pass
  </acceptance_criteria>
  <done>Full test suite passes, anchor.py and pipeline.py imports verified, hallucination guard operational</done>
</task>

<task type="auto">
  <name>Task 2: Verify CLI features and clean stale docstrings</name>
  <read_first>
    - src/pitcher_narratives/cli.py (full file -- understand verbose, print-prompts, hallucination check flow)
    - src/pitcher_narratives/anchor.py (lines 1-5 -- stale docstring referencing report.py)
    - src/pitcher_narratives/config.py (lines 1-5 -- stale docstring referencing report.py)
  </read_first>
  <files>src/pitcher_narratives/anchor.py, src/pitcher_narratives/config.py</files>
  <action>
    **Part A: Verify CLI features work through pipeline path**

    1. Verify --verbose flag exists and is wired:
       `uv run pitcher-narratives --help` and confirm `-v, --verbose` appears.
       Check cli.py line 87: `if args.verbose: _print_verbose_summary(pitcher_data)` is present.

    2. Verify --print-prompts flag exists and is wired:
       `uv run pitcher-narratives --help` and confirm `--print-prompts` appears.
       Check cli.py lines 102-107: `if args.print_prompts:` reads the pipeline data file and prints to stderr.

    3. Verify hallucination check is wired (no CLI flag needed -- it runs automatically):
       Check cli.py line 161: `hallucination_report = check_hallucinated_metrics(pipe_result.narrative)` is present.
       This imports `check_hallucinated_metrics` from `pitcher_narratives.pipeline` (line 92).

    4. Run CLI integration tests to confirm end-to-end:
       `uv run python -m pytest tests/test_cli.py -x -q`

    **Part B: Clean stale docstrings (identified in Phase 23 verification)**

    Phase 23 verification found two informational anti-patterns -- stale docstring references to the now-deleted report.py:

    1. In `src/pitcher_narratives/anchor.py` line 3, change:
       FROM: `Shared by both the single-agent report pipeline (report.py) and the\nmulti-agent specialist pipeline (pipeline.py).`
       TO: `Used by the multi-agent specialist pipeline (pipeline.py) to verify\ncapsule faithfulness to the synthesis.`

    2. In `src/pitcher_narratives/config.py` line 4, change:
       FROM: `Centralises constants and utilities that were previously scattered across\nreport.py, pipeline.py, analyst.py, and the CLI modules.`
       TO: `Centralises constants and utilities shared by pipeline.py, analyst.py,\nand the CLI modules.`

    These are the only two stale references found. Do NOT modify any other files.
  </action>
  <verify>
    <automated>cd /Users/matt/src/pitcher-narratives && uv run python -m pytest tests/test_cli.py tests/test_hallucination_guard.py -x -q && uv run python -c "from pitcher_narratives.anchor import AnchorResult; from pitcher_narratives.config import API_KEYS; print('imports OK after docstring cleanup')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pitcher-narratives --help` shows both --verbose and --print-prompts flags
    - cli.py contains `check_hallucinated_metrics(pipe_result.narrative)` call (automatic hallucination check)
    - CLI tests pass (tests/test_cli.py)
    - anchor.py docstring no longer references report.py
    - config.py docstring no longer references report.py
    - All imports still work after docstring edits
  </acceptance_criteria>
  <done>All CLI features verified working through pipeline path, stale report.py docstring references cleaned</done>
</task>

</tasks>

<verification>
Phase 24 success criteria verification:

1. **Full test suite passes with zero failures** -- `uv run python -m pytest -x -q --ignore=tests/test_analyst.py` exits 0
2. **anchor.py functions importable and used by pipeline.py** -- `python -c "from pitcher_narratives.anchor import AnchorResult, AnchorWarning"` succeeds AND `grep "from pitcher_narratives.anchor import" src/pitcher_narratives/pipeline.py` returns a match
3. **CLI streaming output works through pipeline.py** -- cli.py calls `generate_pipeline_streaming` from pipeline.py (line 124)
4. **Hallucination check works through pipeline path** -- cli.py calls `check_hallucinated_metrics` from pipeline.py (line 161), runs automatically on every report
5. **CLI info modes work through pipeline path** -- `--verbose` prints pitcher summary (line 87-88), `--print-prompts` prints pipeline data file to stderr (lines 102-107)
</verification>

<success_criteria>
- Zero test failures in full suite (excluding pre-existing test_analyst.py)
- anchor.py AnchorResult/AnchorWarning importable and used by pipeline.py
- CLI --verbose, --print-prompts, and automatic hallucination check all functional
- No stale references to deleted report.py in docstrings
- v1.9 Pipeline Consolidation milestone verification complete
</success_criteria>

<output>
After completion, create `.planning/phases/24-verification-cleanup/24-01-SUMMARY.md`
</output>
